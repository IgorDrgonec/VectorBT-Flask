version: "3.9"

services:
  tradingbot:
    build: .
    container_name: tradingbot
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - ENV=production